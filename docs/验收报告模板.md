# 「点时成金」测试与验收报告

> 本报告用于 Demo 交付验收。模板见 §A，基于本次真实运行的已填样例见 §B。
> 自动生成产物：`reports/pytest-report.html`、`reports/coverage.xml`、`reports/summary.txt`。

---

## §A. 验收报告模板（可复制填写）

```markdown
# 「点时成金」测试与验收报告

## 一、项目与版本信息
| 项 | 内容 |
|----|------|
| 项目名称 | 点时成金（黄金价格 30 分钟方向预测系统） |
| 版本 / 构建号 | {{VERSION}} |
| 交付类型 | {{DEMO / 正式}} |
| 测试执行时间 | {{YYYY-MM-DD HH:MM}} |
| 测试负责人 | {{NAME}} |
| 代码分支 / Commit | {{BRANCH @ COMMIT}} |

## 二、测试范围与环境
- 测试范围：{{单元 / 接口 / 集成 / 端到端 / 性能 / 安全 —— 勾选}}
- 执行模式：{{all / smoke / 分层}}
- 环境：Python {{VER}} ｜ OS {{OS}} ｜ 数据库 {{SQLite / PostgreSQL}}
- 隔离说明：{{DATABASE_URL 重定向至测试库；调度 / 实时爬虫关闭}}

## 三、测试执行结果汇总
| 指标 | 数值 |
|------|------|
| 用例总数（selected） | {{TOTAL}} |
| 通过（passed） | {{PASSED}} |
| 失败（failed） | {{FAILED}} |
| 阻塞（blocked） | {{BLOCKED}} |
| 已知缺陷（xfailed，不计入失败） | {{XFAILED}} |
| 通过率（passed / selected） | {{RATE}} % |
| 代码覆盖率（行） | {{COV_LINE}} % |
| 覆盖率（分支） | {{COV_BRANCH}} % |
| 退出码 | {{EXIT_CODE}} |

> 覆盖率明细见 reports/coverage-html/index.html；完整日志见 reports/pytest-output.log。

## 四、缺陷记录表
| 缺陷编号 | 描述 | 严重等级 | 复现步骤 | 影响模块 | 状态 | 责任人 |
|----------|------|----------|----------|----------|------|--------|
| {{DEF-XXX}} | {{描述}} | {{高/中/低}} | {{步骤}} | {{模块}} | {{已修复 / 跟踪中 / 遗留}} | {{NAME}} |
| | | | | | | |

## 五、遗留问题与风险
- {{遗留问题 1：描述 + 影响 + 缓解}}
- {{遗留问题 2：……}}

## 六、验收结论与签署
- 验收结论：{{通过 / 有条件通过 / 不通过}}
- 结论说明：{{依据 —— 如「0 失败、安全红线全过、覆盖率达标」}}

| 角色 | 姓名 | 签署 | 日期 |
|------|------|------|------|
| 测试负责人 | | | |
| 开发负责人 | | | |
| 产品 / 验收方 | | | |
```

---

## §B. 已填样例（基于 {{YYYY-MM-DD}} 真实运行）

# 「点时成金」测试与验收报告（样例）

## 一、项目与版本信息
| 项 | 内容 |
|----|------|
| 项目名称 | 点时成金（黄金价格 30 分钟方向预测系统） |
| 版本 / 构建号 | demo-2026.08.06 |
| 交付类型 | Demo |
| 测试执行时间 | 2026-08-06 23:54 |
| 测试负责人 | 自动化测试套件（WorkBuddy） |
| 代码分支 / Commit | 本地工作区（forecasts for gold prices） |

## 二、测试范围与环境
- 测试范围：✅ 单元 ✅ 接口 ✅ 集成 ✅ 端到端 ✅ 性能基线 ✅ 安全基线（external 真实爬虫按需排除）
- 执行模式：`all`（`not external`，离线可跑）
- 环境：Python 3.13.12 ｜ macOS ｜ SQLite（WAL）
- 隔离说明：`DATABASE_URL` → `.pytest_tmp/gold_test.db`；`DEMO_MODE=true`；`SCHEDULER_ENABLED=false`；`NEWS_SCRAPE_ENABLED=false`

## 三、测试执行结果汇总
| 指标 | 数值 |
|------|------|
| 用例总数（collected） | 268 |
| 排除（deselected / external） | 1 |
| 选中执行（selected） | 267 |
| 通过（passed） | 261 |
| 失败（failed） | 0 |
| 阻塞（blocked） | 0 |
| 已知缺陷（xfailed，不计入失败） | 6（DEF-002~007） |
| 通过率（passed / selected） | 97.75% |
| 代码覆盖率（行） | 58.32% |
| 覆盖率（分支） | 已启用（`branch=True`） |
| 退出码 | 0 |

> 完整数据见 `reports/summary.txt`：`261 passed, 1 deselected, 6 xfailed, 16 warnings in 4.16s`。

## 四、缺陷记录表
| 缺陷编号 | 描述 | 严重等级 | 复现步骤 | 影响模块 | 状态 | 责任人 |
|----------|------|----------|----------|----------|------|--------|
| DEF-001 | 回测「取最新」在 SQLite 秒级时间戳同值时排序不确定，可能取到旧记录 | 高 | 连续写入两条 `created_at` 相同的回测记录，读「最新」接口 | backtest 读接口 | **已修复**（追加 `desc(id)` 次级排序键） | 开发组 |
| DEF-002 | 框架级参数校验 422 返回裸 `detail`，未包统一信封 | 中 | 触发任意 422（如非法类型字段），观察响应结构 | api | 跟踪中（xfail，待加中间件） | 后端 |
| DEF-003 | 非法 `start_date` 被静默忽略，未回传校验错误 | 中 | 传非法日期参数 | api | 跟踪中（xfail） | 后端 |
| DEF-004 | demo 模式无鉴权，任意请求可访问全部接口 | 高（上线前） | 不带 token 直接调用接口 | 全部 | 跟踪中（xfail，上线前必须补） | 后端 / 安全 |
| DEF-005 | CORS `allow_origins=['*']` 全开 | 中（上线前） | 跨域请求任意 Origin 被接受 | api | 跟踪中（xfail） | 后端 |
| DEF-006 | 响应缺少安全头（CSP / HSTS / X-Frame-Options） | 中（上线前） | 检查任意响应头 | 全部 | 跟踪中（xfail） | 后端 |
| DEF-007 | 接口无限流，可被高频刷 | 中（上线前） | 连续高频请求不被拦截 | api | 跟踪中（xfail） | 后端 |

## 五、遗留问题与风险
- **低覆盖率模块（采集 / 情感层）**：`collectors/adapters.py`(21.64%)、`sentiment.py`(22.89%)、`factor_collectors.py`(23.33%)、`news_collector.py`(23.85%)、`data_collector.py`(35.14%)、`signal_generator.py`(36.84%)。这些多为需真实外网 / 模型的采集与推理路径，本方案以 fixture 与单元函数覆盖为主，未做端到端采集回归。建议后续补 `external` 采集回归与模型推理单测。
- **上线前安全门禁（DEF-004~007）**：demo 阶段可接受，但正式上线前必须闭环（鉴权、CORS 收敛、安全头、限流）。
- **UI 端到端为 L1 无浏览器方案**：解析真实 `dashboard.html` + 真后端校验，已覆盖接口可达性与文案合规；若需像素级 / 交互级验证，可启用 `scripts/frontend_integration_test.mjs`（jsdom，标记 `external`）。

## 六、验收结论与签署
- **验收结论：有条件通过（Demo 阶段）**
- **结论说明**：全量用例 **0 失败**，安全红线（SEC-01~09、SEC-14）100% 通过，覆盖率 58.32% 达到 demo 基线；6 个已知缺陷已记录并以 xfail 固化跟踪（DEF-001 已修复，DEF-002~007 为上线前门禁）。可在 demo 环境交付，正式上线前须闭环 DEF-004~007 并补充采集层测试。

| 角色 | 姓名 | 签署 | 日期 |
|------|------|------|------|
| 测试负责人 | （自动套件） | — | 2026-08-06 |
| 开发负责人 | | | |
| 产品 / 验收方 | | | |
