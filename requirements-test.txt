# ============================================================
# 「点时成金」测试专用依赖
# 安装： pip install -r requirements.txt -r requirements-test.txt
#
# 说明：requirements.txt 是运行时依赖；本文件只装测试工具链，
#       保持生产镜像精简（Dockerfile 不安装本文件）。
# ============================================================

# —— 测试框架 ——
pytest>=8.0                 # 测试运行器
pytest-cov>=5.0             # 覆盖率统计（基于 coverage.py）
pytest-html>=4.1            # 自包含 HTML 测试报告
pytest-timeout>=2.3         # 单用例超时保护，防止卡死 CI
pytest-xdist>=3.5           # 可选：-n auto 并行加速

# —— 覆盖率 ——
coverage[toml]>=7.4

# —— 被测代码的最小可导入依赖 ——
# （这些本就在 requirements.txt 中；此处显式列出是为了支持
#   「只装测试子集」的轻量 CI 场景：不装 lightgbm/xgboost/streamlit/playwright）
fastapi>=0.110
httpx>=0.27                 # FastAPI TestClient 底层
sqlalchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0
pandas>=2.2
numpy>=1.26
beautifulsoup4>=4.12        # app/pipeline/scraper.py 解析依赖
lxml>=5.0
joblib>=1.3                 # app/core/predictor.py 模型加载
scikit-learn>=1.4           # 基线模型
apscheduler>=3.10           # app/core/scheduler.py 导入依赖
