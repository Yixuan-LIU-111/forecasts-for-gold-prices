# 黄金价格方向预测 —— 端到端流程依赖
# 安装（建议使用隔离环境，如本项目 venv）：
#   pip install -r requirements.txt

numpy>=1.23
pandas>=1.5
scikit-learn>=1.1
lightgbm>=3.3
xgboost>=1.7
joblib>=1.2
# 可选（仅 analysis.py 绘图需要）：
matplotlib>=3.5
